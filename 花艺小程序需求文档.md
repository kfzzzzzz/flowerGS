# AI 花艺定制小程序 — 需求文档

## 1. 项目概述

一款基于微信小程序的 AI 花艺定制平台。用户通过选择当前心情/场景和喜欢的花艺风格，由 AI 自动生成专属花束设计方案，并以 3D 虚拟仿真的方式 360° 展示花束效果。

核心理念：**以情感驱动花艺设计，用 AI 让每一束花都有故事。**

---

## 2. 目标用户

- 想要定制个性化花束但缺乏花艺知识的普通消费者
- 希望通过花束表达情感、送礼的用户
- 对花艺美学感兴趣、喜欢尝试新事物的年轻群体

---

## 3. 核心功能模块

### 3.1 场景/心情选择

用户进入小程序后，首先选择当前的心情或场景，作为 AI 生成花束的情感输入。

| 分类 | 示例选项 |
|------|---------|
| 天气心情 | 雨天低落、晴天愉悦、雪天浪漫、阴天平静 |
| 情感场景 | 表白告白、生日祝福、毕业纪念、节日庆祝、道歉和好、自我治愈 |
| 情绪状态 | 开心、忧伤、感恩、思念、期待、放松 |

**交互方式：**
- 卡片式选择，每个选项配有插画图标和简短描述
- 支持多选（如"雨天 + 忧伤"组合）
- 可选：用户也可以自由输入一段文字描述心情

### 3.2 花艺风格选择

选择完场景/心情后，进入风格选择页面。系统根据用户选择的心情，推荐适合的风格（也可自由选择）。

| 风格 | 说明 |
|------|------|
| 韩式小清新 | 柔和色调、简约包装、自然感 |
| 法式浪漫 | 丰盈饱满、经典配色、优雅丝带 |
| 日式侘寂 | 极简留白、枝条线条感、禅意 |
| 复古文艺 | 莫兰迪色系、干花混搭、做旧质感 |
| 热烈奔放 | 高饱和色彩、大朵花材、视觉冲击 |
| 森系自然 | 绿植为主、野趣感、不规则造型 |

**交互方式：**
- 横向滑动卡片，每种风格配有代表性花束示例图
- 选中后高亮显示

### 3.3 AI 花束生成

用户确认心情 + 风格后，AI 根据输入生成花束设计方案。

**生成内容包括：**
- 花束名称（AI 起一个有诗意的名字）
- 花材搭配清单（主花、配花、配叶、包装材料）
- 花语描述（一段与用户心情呼应的文案）
- 3D 花束模型（用于虚拟仿真展示）

**技术方案：**
- 调用大语言模型 API（如 GPT / 文心一言）生成花束方案文案
- 调用 AI 图像生成模型生成花束效果图
- 基于预设 3D 模板 + AI 参数映射生成 3D 花束模型

### 3.4 3D 虚拟仿真展示（360° 旋转）

生成花束后，用户进入 3D 展示页面，可以 360° 旋转查看花束的虚拟仿真效果。

**展示要求：**
- 花束为艺术花束造型（非花盆），手持式/包装式呈现
- 支持手指滑动 360° 旋转查看
- 支持双指缩放，查看花材细节
- 花材质感尽量逼真（花瓣纹理、叶片光泽、包装纸褶皱）
- 背景可切换（纯色 / 场景化，如桌面、手持）

**技术方案（MVP 阶段可选）：**

| 方案 | 说明 | 复杂度 |
|------|------|--------|
| 方案 A：序列帧旋转 | 预渲染多角度图片，滑动切换帧实现伪 3D | 低 |
| 方案 B：Three.js WebGL | 小程序内嵌 WebGL，加载 3D 模型实时渲染 | 中 |
| 方案 C：模型查看器 | 接入第三方 3D 模型查看服务（如 Sketchfab） | 中 |

**MVP 建议采用方案 A**，快速验证产品逻辑；后续迭代升级为方案 B。

---

## 4. 页面结构

```
首页（心情选择）
  └── 风格选择页
        └── AI 生成中（加载动画）
              └── 花束展示页
                    ├── 3D 旋转查看区域
                    ├── 花束信息卡片（名称、花材、花语）
                    ├── 重新生成按钮
                    ├── 保存到相册
                    └── 分享给朋友
我的收藏页（历史生成记录）
```

---

## 5. MVP 版本范围

第一版聚焦核心链路，快速上线验证：

**包含：**
- 心情/场景选择（预设选项，不含自由输入）
- 风格选择（4-6 种预设风格）
- AI 生成花束方案（文案 + 效果图）
- 序列帧 360° 旋转查看
- 保存图片 / 分享功能

**不包含（后续迭代）：**
- 用户账号体系 / 登录
- 下单购买真实花束
- 自定义花材替换
- 实时 3D 渲染
- 社区 / 用户作品广场

---

## 6. 技术架构（建议）

```
┌─────────────────────────────────┐
│         微信小程序前端            │
│   (uni-app / 原生小程序开发)      │
│   - 页面交互 & 动画              │
│   - 序列帧/WebGL 3D 展示         │
└──────────┬──────────────────────┘
           │ HTTPS
┌──────────▼──────────────────────┐
│         后端服务 (Spring Boot)    │
│   - 用户请求处理                  │
│   - AI 接口调用编排               │
│   - 花束方案数据管理              │
└──────────┬──────────────────────┘
           │
    ┌──────┴──────┐
    ▼             ▼
┌────────┐  ┌──────────┐
│ LLM API │  │ 图像生成  │
│文案生成  │  │ API      │
└────────┘  └──────────┘
```

---

## 7. 非功能性需求

| 项目 | 要求 |
|------|------|
| AI 生成响应时间 | 文案 < 5s，图片 < 15s |
| 3D 展示流畅度 | 旋转操作 ≥ 30fps |
| 小程序包体积 | 主包 < 2MB |
| 兼容性 | iOS 12+ / Android 7+ / 微信 8.0+ |

---

## 8. 后续扩展方向

- 接入电商：生成的花束方案可一键下单，由合作花店制作配送
- AR 预览：通过手机摄像头将花束放置在真实场景中预览
- 社区功能：用户分享自己的花束设计，互相点赞收藏
- 花艺师入驻：专业花艺师可上传作品，用户可选择花艺师风格
- 订阅制：按周/月推送 AI 花束设计，搭配当季花材
